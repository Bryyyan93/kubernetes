apiVersion: v1
kind: Service
metadata:
  labels:
    app.k8s.io: {{ .Values.posgre.service.name }}
  name: {{ .Values.posgre.service.name }}
spec:
  ports:
    - protocol: {{ .Values.protocol }}
      port: {{ .Values.posgre.service.targetport }}
      targetPort: {{ .Values.posgre.service.targetport }}
  selector:
    app.k8s.io: {{ .Values.posgre.service.name }}