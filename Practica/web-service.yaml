apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    io.app.service: web
spec:
  type: ClusterIP
  ports:
    # - name: "5000"
    - port: 5000 # Puerto del contenedor escucha
      targetPort: 5000 # Puerto interno del contenedor
  selector:
    io.app.service: web
