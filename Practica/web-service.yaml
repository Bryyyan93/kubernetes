apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: web
  name: web
spec:
  type: LoadBalancer
  selector:
    io.kompose.service: web
  ports:
    - protocol: TCP
      port: 5000
      targetPort: http
  #ports:
  #  - name: "5000"
  #    port: 5000
  #    targetPort: 5000
  #selector:
  #  io.kompose.service: web
