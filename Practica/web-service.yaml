apiVersion: v1
kind: Service
metadata:
  labels:
    app.k8s.io: web
  name: web-srv
spec:
  type: ClusterIP
  ports:
    # - name: "5000"
    - port: 5000 # Puerto del contenedor escucha
      targetPort: 5000 # Puerto interno del contenedor
  selector:
    app.k8s.io: web